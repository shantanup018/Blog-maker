<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Blogs</h1>
     <% for(post of posts) {%>
         <div class="post">
            <h3 class="user">@<%= post.username %></h3>
            <h6 class="content"><%= post.content %></h6>
            <br>
            <a class="link1" href="http://localhost:8080/posts/<%= post.id%>">See in Detail</a>
            
            <a class="link1" href="http://localhost:8080/posts/<%= post.id%>/edit">Edit</a>
            
            <form  method="post" action="/posts/<%=post.id%>?_method=DELETE">
                <button class="delete_btn">Delete Blog</button>
            </form>
        </div>
        <hr>
     <% } %>
     <br>
     <br>
     <a href="http://localhost:8080/posts/new" class="link2">Create New Blog</a>

</body>
</html>